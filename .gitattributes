# Normalize line endings so a Windows-authored checkout builds identically on Linux CI.
* text=auto eol=lf

# Windows batch scripts must retain CRLF.
*.bat text eol=crlf

# Binary artifacts — never normalize.
*.jar binary
